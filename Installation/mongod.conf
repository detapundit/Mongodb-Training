# mongod.conf

# Where to store the data.
storage:
  dbPath: "/data"

# Where to log
systemLog:
  destination: file
  logAppend: true
  path: "/var/log/mongodb/mongod.log"

# network interfaces
net:
  port: 27017
  bindIp: 127.0.0.1
